function solution(str) {
  // let answer = ""
  // for (let x of str) {
  //   if (x !== x.toUpperCase()) answer += x.toUpperCase()
  //   else answer += x
  // }
  return str.toUpperCase()
}

console.log(solution("ItisTimeToStudy"))
/**
 * 풀이 시간 : 1:00
 *
 * 풀이 과정 : toUpperCase 사용
 *
 * 풀이 참고 및 Learning :
 * toUpperCase
 */
